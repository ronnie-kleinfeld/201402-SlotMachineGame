{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Run GotchaSlots (Desktop AIR)",
            "command": "${workspaceFolder}/run-desktop.sh",
            "cwd": "${workspaceFolder}"
        }
    ]
}
